<%= render 'items/header' %>

<% if @items.empty? %>
  <p align="center"><em>No result found</em></p>
<% end %>

<%= render 'items/catalog' %>

<% if session[:login] == 1 then %>
  <%= link_to 'New Item', new_item_path %>
<% end %>
